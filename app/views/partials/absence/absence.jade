script#absence(type="text/ng-template")    
    div.container
        div.card
            h4
                | Missing raid or going to be late? Let us know.
            div()
                form(ng-submit = "submitNewAbsence()")
                    div(layout, layout-align = "space-around center")
                        div()
                            md-input-container
                                label Title
                                input(ng-model="user.name", disabled)
                                    
                                
                           
                        div()
                            md-input-container
                                label(for="date").active Date
                                input(id="date", type = "date", required = "required", ng-model = "newAbsence.date").datepicker
                    div(layout)
                        div()
                            label(for="reason") Late or Absent?
                            md-radio-group(ng-model="newAbsence.type", required = "required")
                                md-radio-button(value="late", class="md-primary")
                                    | Late
                                md-radio-button(value="absent") 
                                    | Absent
                            
                    
                    md-input-container.md-block
                        label(for="reason") Reason...   
                        textarea(id = "reason", required = "required", ng-model = "newAbsence.reason")
                              
                    
                    div
                        button(type="submit")#btn-login.btn.btn-primary
                            i.icon-lock.icon-white
                            | Submit
                            